// Warm forest theme: overrides VitePress brand colors with the luna
// palette (bark brown / deep forest / moss / cream). Layout components
// stay stock; only tokens change.
import DefaultTheme from 'vitepress/theme'
import './custom.css'

export default DefaultTheme
